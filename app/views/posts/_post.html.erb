<div class="post-partial">
  <li id="post-<%= post.id %>">
    <%= post.title %>
    <% post.images.each do |image| %>
      <%= image_tag(image, width:100) %>
    <% end %>
    <%= link_to '削除', post, data: { turbo_method: :delete ,turbo_confirm: '削除してよろしいですか?' } %>
  </li>
</div>
